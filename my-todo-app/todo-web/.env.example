# คัดลอกไฟล์นี้เป็น .env แล้วปรับค่าตามเครื่องของคุณ
# ฝั่ง Vite ใช้ prefix VITE_ เท่านั้น

# URL ฐานของ Web API (ไม่มี slash ปิดท้าย) — ตัวอย่างตรงกับ launchSettings profile http
VITE_API_BASE_URL=http://localhost:5102
